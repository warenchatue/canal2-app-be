<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Production Partners Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 0.9rem;
            background: #fff;
            margin: 0;
            padding: 20px;
            color: #000;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            width: 794px; /* A4 width in pixels (approximate) */
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 60px;
        }

        .logo {
            max-width: 130px;
        }

        .header-right {
            text-align: right;
        }

        .header-text {
            font-weight: bold;
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        .header-address {
            margin-bottom: 5px;
        }

        .header-email {
            margin-bottom: 5px;
        }

        .reserved-section {
            text-align: right;
            margin-bottom: 10px;
        }

        .reserved-section .provider {
            color: #ff0000;
            font-weight: bold;
        }

        .production-title {
            text-align: center;
            font-weight: bold;
            color: #ff0000;
            font-size: 1.2rem;
            margin: 20px 0;
        }

        .section {
            margin-bottom: 15px;
        }

        .section-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .section-content {
            margin-left: 10px;
        }

        .timing-table {
            width: 100%;
            margin: 20px 0;
        }

        .signature-section {
            margin-top: 40px;
            text-align: right;
        }

        .commercial-director {
            margin-bottom: 60px;
        }

        .form-fields {
            margin-top: 60px;
        }

        .form-field {
            margin-bottom: 20px;
        }

        .form-field-title {
            margin-bottom: 10px;
        }

        .form-field-line {
            border-bottom: 1px solid #000;
            height: 20px;
        }

        .footer {
            margin-top: 40px;
            text-align: center;
            padding-top: 0;
        }

        .footer-text {
            margin-bottom: 10px;
        }

        .footer-divider {
            border-top: 2px solid #1b9e77;
            margin: 15px auto;
            width: 100%;
        }

        .footer-contact {
            margin-bottom: 10px;
        }

        .footer-legal {
            margin-bottom: 10px;
            padding-top: 0;
        }

        .page-number {
            text-align: right;
            margin-top: 15px;
            margin-bottom: 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header from the previous document -->
        <div class="header">
            <img src="<%= typeof logoUrl !== 'undefined' ? logoUrl : '' %>" alt="Canal 2 International" class="logo">
            <div class="header-right">
                <div class="header-text">Canal2 International</div>
                <div class="header-address">Siège social: Avenue de l'indépendance, derrière le monument du cinquantenaire</div>
                <div class="header-email">Courriel : dircom@cana2international.net</div>
            </div>
        </div>

        <div class="reserved-section">
            <div>PARTIE RESERVE AU PRESTATAIRE : <span class="provider"><%= typeof otherProductionPartner !== 'undefined' ? otherProductionPartner : 'EDEA' %></span></div>
        </div>

        <div class="production-title">PRODUCTION</div>

        <div class="section">
            <div class="section-title">ANNONCEUR :</div>
            <div class="section-content"><%= typeof announcer !== 'undefined' ? announcer.name : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">CONTACT CLIENT :</div>
            <div class="section-content"><%= typeof contactDetails !== 'undefined' ? contactDetails : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">POINT FOCAL :</div>
            <div class="section-content"><%= typeof keyContact !== 'undefined' ? keyContact : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">LIEU DE COUVERTURE :</div>
            <div class="section-content"><%= typeof location !== 'undefined' ? location : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">DATE :</div>
            <div class="section-content"><%= typeof date !== 'undefined' ? new Date(date).toLocaleDateString('fr-FR', {weekday: 'long', year: 'numeric', month: 'long', day: 'numeric'}) : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">COMMERCIAL EN CHARGE /SERVICE :</div>
            <div class="section-content"><%= typeof serviceInCharge !== 'undefined' ? serviceInCharge : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">ESPACE DE DIFFUSION</div>
        </div>

        <div class="section">
            <div class="section-title">NATURE DE LA PRESTATION</div>
            <div class="section-content"><%= typeof natureDescription !== 'undefined' ? natureDescription : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">HEURE PREVUE :</div>
            <div class="section-content"><%= typeof hour !== 'undefined' ? hour : '' %></div>
        </div>

        <div class="section">
            <div class="section-title">HEURE EFFECTIVE</div>
        </div>

        <div class="section">
            <div class="section-title">TIMING</div>
        </div>

        <div class="signature-section">
            <div class="commercial-director">Le directeur commercial</div>
        </div>

        <div class="form-fields">
            <div class="form-field">
                <div class="form-field-title">Nom du journaliste</div>
                <div class="form-field-line"></div>
            </div>

            <div class="form-field">
                <div class="form-field-title">Nom du cameraman</div>
                <div class="form-field-line"></div>
            </div>

            <div class="form-field">
                <div class="form-field-title">Date effective de diffusion</div>
                <div class="form-field-line"></div>
            </div>

            <div class="form-field">
                <div class="form-field-title">Nom et signature du service concerné</div>
                <div class="form-field-line"></div>
            </div>
        </div>

        <!-- Footer from the previous document -->
        <div class="footer">
            <div class="footer-text">Toute l'équipe de Canal2 International vous dit merci !</div>
            <div class="footer-divider"></div>
            <div class="footer-contact">Téléphone : (+237) 657797171 / 672676799 . https://www.canal2international.net</div>
            <div class="footer-legal">N° Contribuable: M030300016713S - RC: 2003B224 ;</div>
        </div>

        <div class="page-number">Page 1/1</div>
    </div>
</body>
</html>