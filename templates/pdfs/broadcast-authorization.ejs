<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Autorisation de Diffusion</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 0.9rem;
            background: #fff;
            margin: 0;
            padding: 20px;
            color: #000;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
        }

        .logo {
            max-width: 130px;
        }

        .title {
            text-align: center;
            font-weight: bold;
            font-size: 1.2rem;
            margin: 20px 0;
        }

        .section {
            margin-bottom: 20px;
        }

        .announcer {
            font-weight: bold;
            margin-bottom: 10px;
        }

        .nature-list {
            margin-top: 10px;
        }

        .nature-columns {
            display: flex;
            flex-wrap: wrap;
        }

        .nature-column {
            width: 50%;
        }

        ul {
            list-style-type: disc;
            padding-left: 20px;
            margin: 5px 0;
        }

        li {
            margin-bottom: 5px;
        }

        .bold {
            font-weight: bold;
        }

        .duration {
            margin: 20px 0;
        }

        .antenna {
            text-align: center;
            font-weight: bold;
            margin: 20px 0;
            text-decoration: underline;
        }

        .taf {
            margin-bottom: 15px;
        }

        .questions {
            margin-top: 15px;
        }

        .question {
            margin-bottom: 15px;
        }

        .client {
            margin: 20px 0;
            font-weight: bold;
        }

        .contact {
            margin: 15px 0;
        }

        .date-time {
            margin: 15px 0;
        }

        .signatures {
            display: flex;
            justify-content: space-between;
            margin-top: 40px;
        }

        .signature {
            width: 45%;
        }

        .signature-line {
            margin-top: 60px;
            border-top: 1px solid black;
        }

        .page-number {
            text-align: right;
            margin-top: 40px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 5px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="<%= typeof logoUrl !== 'undefined' ? logoUrl : '' %>" alt="Canal 2 International" class="logo">
            <div></div>
        </div>

        <div class="title">AUTORISATION DE DIFFUSION</div>

        <div class="section">
            <div class="announcer">ANNONCEUR : <%= typeof announcer !== 'undefined' && announcer ? announcer.name : 'N/A' %></div>
            
            <div>Nature de l'annonce :</div>
            <div class="nature-columns">
                <div class="nature-column">
                    <ul>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Nous Chez vous') ? 'class="bold"' : '' %>>Nous Chez vous !</li>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('canal matin') ? 'class="bold"' : '' %>>canal matin</li>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Anniversaire avec photo') ? 'class="bold"' : '' %>>Anniversaire avec photo</li>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Publi info') ? 'class="bold"' : '' %>>Publi info</li>
                    </ul>
                    <div <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Requiem') ? 'class="bold"' : '' %>>Requiem / <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Décès') ? 'class="bold"' : '' %>Décès</div>
                </div>
                <div class="nature-column">
                    <ul>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Décès avec photo') ? 'class="bold"' : '' %>>Décès avec photo</li>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('PAD') ? 'class="bold"' : '' %>>PAD *</li>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('C Comment') ? 'class="bold"' : '' %>>C Comment ?</li>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Jambo TV') ? 'class="bold"' : '' %>>Jambo TV</li>
                        <li <%= typeof natureDescription !== 'undefined' && natureDescription.includes('Documentaire') ? 'class="bold"' : '' %>>Documentaire</li>
                    </ul>
                </div>
            </div>
            
            <% if (typeof natureDescription !== 'undefined' && natureDescription) { %>
                <div><strong>Description spécifique:</strong> <%= natureDescription %></div>
            <% } %>
        </div>

        <div class="duration">
            Durée de la prestation du ___/___/____ au <%= typeof endDate !== 'undefined' ? new Date(endDate).toLocaleDateString('fr-FR') : '___/___/____' %>
        </div>

        <div class="antenna">MISE À L'ANTENNE :</div>

        <div class="taf">
            <span class="bold">TAF :</span> Intervention de <%= typeof duration !== 'undefined' ? duration : '___' %> min 
            <span class="bold"><%= typeof description !== 'undefined' ? description : '' %></span> à 
            <span class="bold"><%= typeof hour !== 'undefined' ? hour : '___' %></span>
        </div>

        <div class="section">
            <div class="bold">INTERVENANTS :</div>
            <% if (typeof participants !== 'undefined' && participants.length > 0) { %>
                <%= participants.join(', ') %>
            <% } else { %>
                _______________
            <% } %>
        </div>

        <div class="questions">
            <div class="bold">QUESTIONS :</div>
            <% if (typeof questions !== 'undefined' && questions.length > 0) { %>
                <% questions.forEach(function(question, index) { %>
                    <div class="question"><%= question %></div>
                <% }); %>
            <% } else { %>
                <div class="question">_______________________________________________________</div>
                <div class="question">_______________________________________________________</div>
                <div class="question">_______________________________________________________</div>
            <% } %>
        </div>

        <div class="client">CLIENT :