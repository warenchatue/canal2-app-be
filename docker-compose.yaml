version: '3'
services:
  dinoes-be-staging:
    container_name: dinoes-staging
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '8800:8800'
    volumes:
      - .:/src:cached